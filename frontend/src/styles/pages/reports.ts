import { styled } from "..";

export const Container = styled("main", {
  display: "flex",
  flexDirection: "column",
  width: "100vw",
  alignItems: "center",
  marginBottom: "3rem",

  strong: {
    fontSize: "$lg",
  },
});
